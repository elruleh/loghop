skips:
  - B101  # assert used
  - B110  # try except pass
  - B112  # try except continue
  - B404  # subprocess import is intentional
  - B603  # subprocess calls use validated provider paths without shell=True
  - B607  # provider executables are discovered from PATH or validated absolute paths
  - B105  # '.secret' is a filename, not a password
exclude_dirs: ['tests', '.venv']
